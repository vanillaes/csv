name: Latest

on:
  push:
    branches:
      - main
    paths-ignore:
      - package.json
      - package-lock.json
    tags-ignore:
      - 'v*'

jobs:
  ci:
    uses: vanillaes/.github/.github/workflows/verify.yml@main
