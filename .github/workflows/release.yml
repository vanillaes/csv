name: Release

on:
  push:
    tags:
    - 'v*'

jobs:
  ci:
    uses: vanillaes/.github/.github/workflows/verify.yml@main

  cd-npm:
    needs: ci
    uses: vanillaes/.github/.github/workflows/npm.yml@main

  cd-jsr:
    needs: ci
    uses: vanillaes/.github/.github/workflows/jsr.yml@main
